# Implementation Plans

Generated by the `improve` skill on 2026-07-29 against commit `fa5fd1d`. Execute in the order below unless dependencies say otherwise. Each executor must read its plan fully, run its drift check first, honor STOP conditions, and update only its status row when done.

## Execution order and status

| Plan | Title | Priority | Effort | Depends on | Status |
|---|---|---|---|---|---|
| [001](001-validate-batch-size.md) | Reject invalid batch sizes | P1 | S | — | DONE |
| [002](002-set-po-locale-metadata.md) | Set authoritative PO locale metadata | P1 | M | — | DONE |
| [003](003-preserve-gettext-plurals.md) | Preserve and translate gettext plural forms | P1 | M | 002 | DONE |
| [004](004-route-multi-locale-output.md) | Route multi-locale output distinctly | P1 | M | — | DONE |
| [005](005-separate-readme-context.md) | Separate readme context metadata | P1 | S | — | DONE |
| [006](006-send-gettext-context.md) | Send gettext message context | P2 | S | — | DONE |
| [007](007-enforce-output-integrity.md) | Enforce protected-token integrity | P1 | M | 005 | DONE |
| [008](008-gate-distributable-cli.md) | Gate the distributable CLI in CI | P2 | M | — | DONE |
| [009](009-make-runs-failure-safe.md) | Add atomic output and resumable batches | P2 | M | 007 | DONE |
| [010](010-bound-locale-concurrency.md) | Bound and settle locale concurrency | P2 | M | 004, 009 | DONE |
| [011](011-document-v0-2-surface.md) | Document the complete public surface | P3 | S | 003, 004, 008, 010 | DONE |
| [012](012-remove-unused-dependencies.md) | Remove unused direct dependencies | P3 | S | 008 | DONE |

Status values: `TODO` | `IN PROGRESS` | `DONE` | `BLOCKED` (add a one-line reason) | `REJECTED` (add a one-line rationale).

## Recommended execution waves

```text
Wave 1: 001  002  004  005  006  008
              │         │         │
Wave 2:      003       007       012
                         │
Wave 3:                 009
                    ┌────┴────┐
Wave 4:             010      (review all P1/P2 behavior)
                       │
Wave 5:               011
```

The diagram indicates dependency flow, not authorization to merge branches automatically. If plans are executed in separate branches/worktrees, rebase each dependent plan onto its completed prerequisites before running its drift check.

## Dependency notes

- 003 requires 002 because plural translation needs an authoritative target-locale form count and gettext rule.
- 007 requires 005 so readme context is no longer mixed into the text whose protected-token integrity is validated.
- 009 requires 007 because checkpoints must never persist structurally invalid model output.
- 010 requires 004 so concurrency cannot amplify output-path collisions, and 009 so partial successes remain durable.
- 011 runs late so public documentation describes the completed behavior instead of planned behavior.
- 012 requires 008 so the distributable package gate proves the removed dependencies were truly unused.

## Repository verification baseline

Run these after every source-changing plan:

| Gate | Command | Baseline at `fa5fd1d` |
|---|---|---|
| Typecheck | `pnpm typecheck` | exit 0 |
| Tests | `pnpm test` | 5 files, 64 tests pass |
| Coverage | `pnpm test:coverage` | 89.6% statements, 78.63% branches |
| Build | `pnpm build` | required by Plan 008 and later package-sensitive plans |

The project uses Node 20+, pnpm 10, strict TypeScript ESM, Vite, and Vitest. Match existing style: single quotes, no semicolons, trailing commas, plain actionable `Error` messages, mocked AI calls, and temporary-directory cleanup.

## Findings considered and rejected or deferred

- **Standalone prompt-injection plan**: rejected as a duplicate. The model has no privileged tool access in this package; deterministic handling of instruction-like/malformed model output belongs in Plan 007.
- **Immediate AI SDK 4 → 7 / OpenRouter provider migration**: deferred. Registry inspection showed major-version lag, but no reachable high/critical advisory. Migration risk is material because generation, schema, usage, and provider APIs move together; reconsider after Plans 007 and 008 establish stronger boundaries.
- **Shared PO/readme pipeline refactor**: deferred until correctness and failure behavior are characterized. Refactoring both orchestration paths before Plans 003, 007, and 009 would increase risk without user-visible value.
- **New lint/formatter toolchain**: rejected for now. The repository is small, strict typechecking and tests are fast; the package/build CI gap in Plan 008 has higher leverage.
- **Moderate/low production audit advisories**: no standalone plan. The moderate `jsondiffpatch` HTML formatter path and low file-upload path are not used by this CLI. Re-audit during the coordinated AI SDK migration.
- **Maintainer guide/AGENTS.md**: deferred. Useful, but lower leverage than making the consumer README and distributable verification accurate.

## Index maintenance rules

- Executors update only their own status row unless explicitly assigned index ownership.
- `BLOCKED` must include the exact STOP condition encountered.
- `DONE` requires every machine-checkable done criterion in the plan to pass.
- If source drift invalidates a plan, mark it `BLOCKED — drift at <path>` and request plan reconciliation rather than improvising.
